ls - what is in a directory

  ls                     the current directory
  ls /bin                a named one

Directories are marked; sizes are shown for files. `tree` shows a
directory and everything under it at once.
