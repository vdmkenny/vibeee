exit - leave this shell, and get a fresh one

  exit

There is nothing to exit to: this is the only shell. What makes the
command useful is that init supervises it and starts another, so
`exit` is how a shell that has got itself into a state is recovered.
`off` powers down; `reboot` restarts.
