backlight - how bright the panel is

  backlight              the level now
  backlight 50           set it

The firmware owns the scale, so the numbers a machine accepts are its
own. Fn+F3 and Fn+F4 do the same through the platform service.
