backlight - how bright the panel is

  backlight              the level now
  backlight 50           set it

The firmware owns the scale, so the numbers a machine accepts are its
own. Fn+F3 and Fn+F4 move the panel too, and the firmware moves it: the
level they leave it at is not known here until something asks. `man
hotkeys` says why.
