net - the network interfaces

  net                    every interface: link, counters, address
  net <iface> up         enable that interface, persistently
  net <iface> down       disable it
  net <iface> dhcp       clear any static claim; DHCP asks
  net <iface> static 192.0.2.50/24 gw 192.0.2.1 dns 192.0.2.1
                         claim an address; gw and dns are optional
  net <iface> join <name> [<key>]
                         join a wireless network; the key is a passphrase
                         of 8 to 63 characters or 64 hex digits, and is
                         left out for an open network
  net <iface> default    send traffic with no route of its own this way
  net <iface> channel 6  hold the radio there; 0 sweeps the band
  net <iface> forget     stop joining it
  net <iface> scan       every network that radio has heard: channel,
                         signal and security
  net scan               the same, of whichever radio this machine has

An interface is named the way the listing prints it ("e1000",
"e1000.1" for a second card of the same kind), by class ("ether",
"wifi"), or by its bus location ("03:00.0"). Class names cover
whichever interfaces no more specific entry claims.

Configuration lives in four slots of the settings store and applies
at once: it outlives the command, and `cfg net` shows what is
recorded. Naming an interface edits the slot that matches it, or
takes a free slot; an emptied match (cfg net if0_match "") frees
its slot again.

With no static claim, an enabled interface asks DHCP as soon as its
cable carries, and prints the address when it arrives. An interface
no slot covers stays down.

`default` is a wish rather than a rule. An interface that is down or has
no address cannot carry the traffic that has no route of its own, so one
that can carries it instead, and the named interface takes it back when
it is able again. With nothing named, the machine chooses in slot order,
which gives it to the wired slot without anyone saying so. This is what
to set on a machine whose radio and wired port are both up and should
not both be answering for the whole network.

`up` and `down` are about the interface and not about the hardware: a
radio taken down here keeps its power, which is what leaves `net scan`
able to hear anything. `hw wireless off` cuts the power, and the
wireless key is wired to do both in the order that keeps them agreeing,
for a machine whose firmware hands the key over (`man hotkeys`). A radio
whose power is off is on no bus at all, so it is absent from `devices`
rather than shown as an interface that is down.

A radio scans whenever it is not joined, and the listing says which
channel it is on. Every verb here has the same control in the
Settings window's Network pane and in the bar's network menu, and
`cfg net` shows the slot they all edit.

  net -p <address>       one ARP probe beneath the stack; the next
                         `net` shows which machine answered

Settings, generated from the schema: net

  net.hostname              unset
      letters, digits and hyphens; unset takes vibeee-<address>
  net.if0_match             ether
      a class (ether, wifi), a driver name, a bus location, or unset
  net.if0_enabled           true
      true | false
  net.if0_default           false
      true | false
  net.if0_address           unset
      an address and prefix, a.b.c.d/nn; unset asks DHCP
  net.if0_gateway           unset
      an address, a.b.c.d; unset takes what the lease offers
  net.if0_dns               unset
      up to two addresses, comma separated
  net.if0_ssid              unset
      a network name, up to 32 characters
  net.if0_psk               unset
      a passphrase of 8 to 63 characters, or 64 hex digits
  net.if0_channel           0
      a number
  net.if0_regdomain         conservative
      conservative | fcc | etsi | mkk | unrestricted
  net.if0_txpower           regulatory
      regulatory | max | a number of dBm
  net.if1_match             wifi
      a class (ether, wifi), a driver name, a bus location, or unset
  net.if1_enabled           true
      true | false
  net.if1_default           false
      true | false
  net.if1_address           unset
      an address and prefix, a.b.c.d/nn; unset asks DHCP
  net.if1_gateway           unset
      an address, a.b.c.d; unset takes what the lease offers
  net.if1_dns               unset
      up to two addresses, comma separated
  net.if1_ssid              unset
      a network name, up to 32 characters
  net.if1_psk               unset
      a passphrase of 8 to 63 characters, or 64 hex digits
  net.if1_channel           0
      a number
  net.if1_regdomain         conservative
      conservative | fcc | etsi | mkk | unrestricted
  net.if1_txpower           regulatory
      regulatory | max | a number of dBm
  net.if2_match             unset
      a class (ether, wifi), a driver name, a bus location, or unset
  net.if2_enabled           false
      true | false
  net.if2_default           false
      true | false
  net.if2_address           unset
      an address and prefix, a.b.c.d/nn; unset asks DHCP
  net.if2_gateway           unset
      an address, a.b.c.d; unset takes what the lease offers
  net.if2_dns               unset
      up to two addresses, comma separated
  net.if2_ssid              unset
      a network name, up to 32 characters
  net.if2_psk               unset
      a passphrase of 8 to 63 characters, or 64 hex digits
  net.if2_channel           0
      a number
  net.if2_regdomain         conservative
      conservative | fcc | etsi | mkk | unrestricted
  net.if2_txpower           regulatory
      regulatory | max | a number of dBm
  net.if3_match             unset
      a class (ether, wifi), a driver name, a bus location, or unset
  net.if3_enabled           false
      true | false
  net.if3_default           false
      true | false
  net.if3_address           unset
      an address and prefix, a.b.c.d/nn; unset asks DHCP
  net.if3_gateway           unset
      an address, a.b.c.d; unset takes what the lease offers
  net.if3_dns               unset
      up to two addresses, comma separated
  net.if3_ssid              unset
      a network name, up to 32 characters
  net.if3_psk               unset
      a passphrase of 8 to 63 characters, or 64 hex digits
  net.if3_channel           0
      a number
  net.if3_regdomain         conservative
      conservative | fcc | etsi | mkk | unrestricted
  net.if3_txpower           regulatory
      regulatory | max | a number of dBm
