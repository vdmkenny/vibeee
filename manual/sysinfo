sysinfo - any fact the kernel answers, by name

  sysinfo                list every key
  sysinfo <key>          print one answer

The kernel describes itself through one interface, and every window and
tool reads from it; this prints the same answers raw.

The list comes from the kernel rather than from here, so it is whatever
this build actually answers. A key it does not know is refused rather
than answered blankly.
