#ifndef _SYS_TIME_H
#define _SYS_TIME_H
/* The struct and the call live in <time.h> here: there is one clock and one
 * place to ask it, and splitting the question across two headers is an
 * accident of history rather than a distinction. */
#include <time.h>
#endif
