# Generated from src/lib/driver.zig by `zig build driver-manifests`.
# Do not edit: change what the driver answers for instead.
#
# The companion controller, by class: full and low speed devices, which
# is what a keyboard or a mouse on a root port turns out to be.
name    = uhci
service = usb
match   = pci-class:0c:03:00
